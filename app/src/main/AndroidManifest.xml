<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.utng.springtutorial">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/springlogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioActivity" />
        <activity android:name=".MenuActivity"></activity>
        <activity android:name=".TestActivity" />
        <activity android:name=".TemarioActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".configuracion.PreferenciasActivity" />
        <activity android:name=".configuracion.SeleccionPreferenciasActivity" />
        <activity android:name=".login.LoginInterno" />
        <activity android:name=".login.ReadComments" />
        <activity android:name=".login.FormularioInterno" />
        <activity android:name=".CapitulosActivity" />
        <activity android:name=".login.PreguntaActivity" />
        <activity android:name=".TabsActivity" />
        <activity android:name=".ActividadVideo" />
        <activity android:name=".grafica.GraficoActivityDos" />
        <activity android:name=".ResultadoActivity" />
        <activity android:name=".Grafica" />
        <activity android:name=".correo.EnviarCorreoActivity"></activity>
        <activity android:name=".rspgame.ActivityRsp"></activity>
        <activity android:name=".utileries.ActivityCalculator"></activity>
        <activity android:name=".utileries.ActivityCronometro"></activity>
        <activity android:name=".utileries.ActivityUtilerias"></activity>
        <activity android:name=".pagutng.UtngActivity"></activity>
        <activity android:name=".pagutng.GalleryActivity"></activity>
        <activity android:name=".pagutng.Pop"
            android:theme="@style/AppTheme.CustomTheme">
        </activity>
        <activity android:name=".pagutng.Pop2"
            android:theme="@style/AppTheme.CustomTheme">
        </activity>
        <activity android:name=".pagutng.Pop3"
            android:theme="@style/AppTheme.CustomTheme">
        </activity>
        <activity android:name=".sound.SoundActivity"></activity>


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"

            ></activity>


    </application>

</manifest>
